package linear.util;

/**
 * Created by guangshuozang on 8/26/15.
 * This is pseudocode, not used anywhere
 */
public class DFS {
    /*
    void search(TreeNode root){
        if(root == null) return;
        visit(root);
        root.visited = true;
        foreach(TreeNode n in root.adjacent){
            if(n.visited == false){
                search(n);
            }
        }
    }

    void searchWithStack(TreeNode root){
        if(root == null) return;
        visit(root);
        root.visited = true;
        stack.push(root);
        searchWithStack(root.smallestUnvisitedAdjacent);
        stack.pop;
    }

    */

}

